# Evidence log

Capture path: `qm guest exec 100` from the Proxmox host shell (root@proxmox), virtio serial channel, no WinRM/RDP involved. All commands run 2026-08-31, ~15:57 UTC, same session, back to back.

| Evidence file | Command (verbatim) | Timestamp (UTC) |
|---|---|---|
| `domain-admins-membership.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroupMember -Identity 'Domain Admins' \| Select-Object Name,SamAccountName,objectClass \| ConvertTo-Json"` | 2026-08-31T15:57:36Z |
| `enterprise-admins-membership.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroupMember -Identity 'Enterprise Admins' \| Select-Object Name,SamAccountName,objectClass \| ConvertTo-Json"` | 2026-08-31T15:57:36Z |
| `local-administrators-group-lookup.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-LocalGroupMember -Group 'Administrators' \| Select-Object Name,ObjectClass,PrincipalSource \| ConvertTo-Json"` | 2026-08-31T15:57:36Z |
| `adws-service-state.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-Service ADWS \| Select-Object Name,Status,StartType \| ConvertTo-Json"` | 2026-08-31T15:57:36Z |
| `builtin-administrators-membership.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroupMember -Identity 'Administrators' \| Select-Object Name,SamAccountName,objectClass \| ConvertTo-Json"` | 2026-08-31T16:00:32Z |
| `sysadmin-memberof-precheck.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADUser -Identity 'sysadmin' -Properties MemberOf,adminCount \| Select-Object Name,SamAccountName,adminCount,MemberOf \| ConvertTo-Json"` | 2026-08-31T16:09:02Z |
| `sg-admin-tier0-domain-lookup.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroup -Identity 'SG_admin_tier0_domain' -Properties memberOf,Description \| Select-Object Name,Description,memberOf \| ConvertTo-Json"` | 2026-08-31T16:09:02Z |
| `vm100-pre-removal-snapshot.txt` | `qm listsnapshot 100` then `qm snapshot 100 pre-sysadmin-admin-removal-20260831 --description "..."` (host shell, not guest exec) | 2026-08-31T16:12:03Z |
| `sysadmin-removal-attempt-1-failed.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Remove-ADGroupMember -Identity 'Administrators' -Members 'sysadmin' -Confirm:$false"` | 2026-08-31T16:14:16Z |
| `builtin-administrators-membership-post-attempt1.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroupMember -Identity 'Administrators' \| Select-Object Name,SamAccountName,objectClass \| ConvertTo-Json"` | 2026-08-31T16:14:16Z |
| `sysadmin-admincount-post-attempt1.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADUser -Identity 'sysadmin' -Properties adminCount \| Select-Object Name,SamAccountName,adminCount \| ConvertTo-Json"` | 2026-08-31T16:14:16Z |

| `sysadmin-removal-attempt-2-succeeded.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Remove-ADGroupMember -Identity 'Administrators' -Members 'sysadmin' -Confirm:\$false"` (note the escaped `\$false`, fixing attempt 1's bug) | 2026-08-31T16:14:59Z |

| `builtin-administrators-membership-post-removal.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADGroupMember -Identity 'Administrators' \| Select-Object Name,SamAccountName,objectClass \| ConvertTo-Json"` | 2026-08-31T16:15:52Z |
| `sysadmin-admincount-post-removal.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADUser -Identity 'sysadmin' -Properties adminCount \| Select-Object Name,SamAccountName,adminCount \| ConvertTo-Json"` | 2026-08-31T16:15:52Z |
| `sysvol-restricted-groups-sg-admin-tier0-grep.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ChildItem -Path 'C:\Windows\SYSVOL\domain\Policies' -Recurse -Include GptTmpl.inf,Groups.xml -ErrorAction SilentlyContinue \| Select-String -Pattern 'SG_admin_tier0_domain' -SimpleMatch \| Select-Object Path,LineNumber,Line \| ConvertTo-Json"` | 2026-08-31T16:36:28Z |
| `dc-ou-linked-gpos.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-GPInheritance -Target 'OU=Domain Controllers,DC=district,DC=local' \| Select-Object -ExpandProperty GpoLinks \| Select-Object DisplayName,GpoId,Enabled,Enforced,Order \| ConvertTo-Json"` | 2026-08-31T16:36:28Z |
| `sysvol-restricted-groups-sg-admin-tier0-sid-grep.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "\$sid = (Get-ADGroup -Identity 'SG_admin_tier0_domain' -Properties objectSid).objectSid.Value; \$hits = Get-ChildItem -Path 'C:\Windows\SYSVOL\domain\Policies' -Recurse -Include GptTmpl.inf,Groups.xml -ErrorAction SilentlyContinue \| Select-String -Pattern \$sid -SimpleMatch \| Select-Object Path,LineNumber,Line; [PSCustomObject]@{Sid=\$sid; Hits=\$hits} \| ConvertTo-Json -Depth 4"` | 2026-08-31T17:02:48Z |

| `sysadmin-admincount-recheck-t-plus-49min.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADUser -Identity 'sysadmin' -Properties adminCount \| Select-Object Name,SamAccountName,adminCount \| ConvertTo-Json"` | 2026-08-31T17:05:03Z |

Note on the recheck: run ~49 minutes after the 16:15:52Z removal, to test the report's Open Questions claim that `adminCount` might self-clear on the SDProp cleanup cycle. Result unchanged (`adminCount: 1`). Not evidence that self-clear doesn't happen — the ~hourly SDProp cycle interval cited in the report was never independently verified for this lab (it's documented Microsoft behavior, cited as interpretation), so 49 minutes may simply be short of whenever this domain's cycle actually runs. Narrows the question rather than closing it: a self-clear, if one happens, took longer than 49 minutes.

Note on this capture: this command was first issued at ~16:38 UTC, before VM 100's unexpected shutdown (see `exercises/2026-08-31-dc01-unexpected-shutdown/`); it didn't run then because DC01 was down. Re-issued unchanged after the VM was confirmed back up, and ran cleanly on the retry.

Note on the SYSVOL grep: `exited:1, exitcode:0` with no `out-data` key is the expected shape for zero `Select-String` matches — `ConvertTo-Json` on an empty pipeline produces no output at all, not an empty array. Filed as-is, not reconstructed.

Note on attempt 1: the command as given to Raymond had a quoting bug -- `$false` inside the bash-side double-quoted string gets interpolated by bash itself (as an unset variable, i.e. empty string) before `qm guest exec` ever sees it, so PowerShell received a bare `-Confirm:` with no value. Claude's error, caught immediately by the exit-code-1 result rather than assumed to have succeeded. The two follow-up verification commands confirm no change occurred: `sysadmin` is still listed, `adminCount` is still 1, DC01 is in the same state as before the attempt.

Note on timestamps: pasted in by Raymond after running interactively; the log timestamp is when Claude filed the evidence, not a per-command server-side clock reading. The first four captures are one window; `builtin-administrators-membership.json` is a follow-up ~3 minutes later prompted by the local-group failure; `sysadmin-memberof-precheck.json` was pasted twice (first paste truncated mid-JSON and was discarded, not filed — the version here is the complete repaste) and `sg-admin-tier0-domain-lookup.json` was run twice by Raymond with identical output both times, filed once.

| `sysadmin-admincount-recheck-t-plus-5h40m.json` | `qm guest exec 100 --timeout 30 -- powershell.exe -Command "Get-ADUser -Identity 'sysadmin' -Properties adminCount \| Select-Object Name,SamAccountName,adminCount \| ConvertTo-Json"` (identical command to the T+49min recheck) | ~2026-08-31T21:56Z |

Note on the T+5h40m recheck: run in a separate follow-up session picking the exercise back up, per Raymond's request to close out the SDProp open question. Same command as the T+49min recheck, unchanged. Timestamp is approximate — the filing time in this session (this Mac's UTC clock), not a server-printed reading, same methodology as every other timestamp in this log. Elapsed time from the 16:15:52Z removal is ~5 hours 40 minutes, i.e. several multiples of AD's documented ~60-minute SDProp cycle interval (that interval itself is still not independently verified for this domain — see report). Result: `adminCount` still `1`, unchanged from both prior reads.
