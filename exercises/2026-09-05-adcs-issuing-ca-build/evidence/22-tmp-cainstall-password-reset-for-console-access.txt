Command: qm guest exec 100 --timeout 60 -- powershell.exe -NonInteractive -Command '$p = -join
((48..57)+(65..90)+(97..122)+(33,35,36,37,38,42) | Get-Random -Count 20 | ForEach-Object
{[char]$_}); Set-ADAccountPassword -Identity tmp-cainstall -NewPassword (ConvertTo-SecureString
$p -AsPlainText -Force) -Reset; Unlock-ADAccount -Identity tmp-cainstall -ErrorAction
SilentlyContinue; Enable-ADAccount -Identity tmp-cainstall; Write-Output $p'; qm guest exec 100
--timeout 60 -- powershell.exe -NonInteractive -Command "Get-ADUser tmp-cainstall -Properties
PasswordLastSet,LockedOut,Enabled | Select-Object SamAccountName,Enabled,LockedOut,
PasswordLastSet | ConvertTo-Json -Compress"
Host: Proxmox host shell, acting on DC01 (VM 100)
UTC: 2026-09-07T13:52:14Z (PasswordLastSet, derived from the captured .NET date value below)

Context: no enabled account had a working console logon to CA01. The built-in Administrator
password set at Windows install did not work. tmp-cainstall holds local administrator on CA01
and was the untested alternative. Reset its password to a known value to get a console logon.

A first attempt failed before reaching AD. The `-Command` argument was double-quoted at the bash
level, so bash expanded `$p` and `$_` as its own empty variables before PowerShell ever saw the
command. Exit code 1, `CommandNotFoundException` on a bare `=`. No AD write happened. See
Corrections in evidence-log.md. Retried with the PowerShell command single-quoted in bash.

--- password reset, retried with correct quoting ---
{
   "exitcode" : 0,
   "exited" : 1,
   "out-data" : "\r\n"
}

The password line was redacted by Raymond before pasting, per the same rule used in
evidence/09. The value never entered this session.

--- verify ---
{
   "exitcode" : 0,
   "exited" : 1,
   "out-data" : "{\"SamAccountName\":\"tmp-cainstall\",\"Enabled\":true,\"LockedOut\":false,\"PasswordLastSet\":\"\\/Date(1788789134961)\\/\"}\r\n"
}

PasswordLastSet moved from 1788644995481 (2026-09-05 account creation) to 1788789134961
(2026-09-07T13:52:14Z), confirming the reset took effect. Account stayed enabled and unlocked.
The new password was saved to the lab's Vaultwarden instance (container 103), reached by SSH
tunnel from the Mac Mini. Not itself evidence for this repo; the tunnel and Vaultwarden
deployment are covered in exercises/2026-09-03-vaultwarden-secrets-store.
